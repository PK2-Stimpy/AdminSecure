package me.pk2.adminsecure.config.webhook.object.embed;

public class EmbedObjectUrl implements EmbedObject {
    public String url;
    public EmbedObjectUrl(String url) {
        this.url = url;
    }
}